# ============================================
# Template App Environment Configuration
# ============================================

# OPTIONAL: Standalone mode flag
# Controls how Tailwind CSS scans for component source files.
# - Set to "false" when developing in the monorepo (uses local packages)
# - Omit or set to "true" when using as standalone npm package (uses node_modules)
# Default: true (assumes standalone/npm package installation)
# STANDALONE_MODE=false

# REQUIRED: Portfolio username to display
# This is the username of the portfolio you want to showcase in this template app.
# Example: NEXT_PUBLIC_USERNAME=johndoe
NEXT_PUBLIC_USERNAME=your_username_here

# REQUIRED: Backend API base URL
# The base URL of the Portfolioly backend API.
# Default for local development: http://localhost:8000
# For production, use your deployed backend URL (e.g., https://api.portfolioly.com)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# OPTIONAL: Public token for authenticated access
# This token allows access to private portfolios and enables chat functionality.
# Get this token from your portfolio settings in the main app.
# If not provided, only public portfolios will be accessible.
# Format: psk_your_token_here
# Note: When deploying via Vercel button, this is automatically pre-filled
NEXT_PUBLIC_PUBLIC_TOKEN=
